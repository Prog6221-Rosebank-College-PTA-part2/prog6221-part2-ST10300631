<Page x:Class="Chatbot.Bot"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Chatbot"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Bot">



    <Grid>
        <MediaElement x:Name="BackgroundVideo" 
                      Source="Backgrounds/live_bg.mp4" 
                      LoadedBehavior="Manual" 
                      UnloadedBehavior="Manual" 
                      Stretch="UniformToFill" 
                      IsMuted="True"
                      MediaEnded="BackgroundVideo_MediaEnded"/>

        <Grid Background="#55000000">

            <!-- Speech bubble rectangle -->
            <StackPanel>
                <Label Content="Chatting app" FontSize ="24"/>
                <Label Content="description of chatting"/>
                <ListBox x:Name="ListChat" HorizontalContentAlignment="Stretch" BorderThickness="0" Height="382">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <!-- Grid stretches across the ListBox -->
                            <Grid Margin="5" HorizontalAlignment="{Binding Alignment}" >
                                <Border CornerRadius="10" Padding="10,6" Background="{Binding BackgroundColor}">
                                    <TextBlock Text="{Binding Text}" Foreground="{Binding ForegroundColor}" FontSize="14" TextWrapping="Wrap" MaxWidth="300"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <TextBox  Name ="txtMessage" Height="30" />


                <Button Click="OnButtonClick" Width="100" HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                        <!-- Text -->
                        <TextBlock  Text="Send Chat" ></TextBlock>
                        <!-- Icon: Ensure the image is added to your project and "Build Action" is set to "Resource" -->
                        <TextBlock Text="&#xf1d8;" FontSize="20" FontFamily="./#Font Awesome 7 Free Solid"  VerticalAlignment="Center" Margin="5"/>
                    </StackPanel>
                </Button>

            </StackPanel>

        </Grid>

    </Grid>
    
</Page>
